name: Sever MCP
start:
  cmd: node index.js
  env:
    PORT: 3000
    API_KEY: "{{ apiKey }}"

options:
  apiKey:
    type: string
    description: Chave de acesso à API do MCP
    default: ""
    sensitive: true
